<h1>New import</h1>

<% form_for(@import, :html => { :multipart => true }) do |f| %>
  <%= f.error_messages %>
  
  <p>
  	<%= f.label :filename, 'CSV file:' %>
  	<%= f.file_field :filename -%>
  </p>
  
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', imports_path %>